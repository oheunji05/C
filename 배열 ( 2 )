#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main() {

	//문자열의 길이 출력
	char str[20] = "Hello World";
	int i = 0;
	while (str[i] != '\0') {
		i++;
	}
	printf("%d개 글자입니다", i);
  
  return 0;
  
  }
