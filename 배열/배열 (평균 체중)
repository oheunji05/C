#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(){

	//6명의 체중을 입력받아 평균을 출력하는 프로그램을 작성
	//출력은 반올림하여 소수 첫째자리까지로 한다
	 
	double arr[5]={0},sum=0;

  printf("체중 입력 : "); 

	for (int i=0;i<6;i++){
		scanf("%lf",&arr[i]);
		sum=sum+arr[i];
	}

	printf("%.1f",sum/6);

	return 0;
}
