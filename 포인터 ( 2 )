#define _CRT_NO_WARNINGS
#include <stdio.h>
int main (){
	
	int num1=100,num2=100;
	int* pNum;
	pNum =&num1;
	(*pNum)=50;//num1값을 50으로 교체 
	pNum=&num2;
	(*pNum)=-30;//num2값을 -30으로 교체
	printf("%d %d\n",num1,num2);
	 
	return 0;
}
